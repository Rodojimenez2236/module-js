// 1.Dado un string de palabras, devolver la palabra más larga.

    // a. get string
    // b. put sting in an array
    // c. get lenght of items in the array
    // d. sort items by length
    // e. print longest word 

let str ="This seems to take me a long time"

function longestWord(string) {
    const array= string.split(" ")
    return array
}

console.log(longestWord(str))
